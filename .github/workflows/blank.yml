name: Test

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: unittest
      run: |
        cd services && sudo -H sh install_reqs.sh
        set -e
        sudo -H python -m pytest --ignore=integration_tests
        
